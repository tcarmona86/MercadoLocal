import { Form } from "react-bootstrap";
export default function SidebarFiltros({categorias,categoria,onCategoria,orden,onOrden}){return <aside className="filter-panel"><h2>Filtros</h2><Form.Group className="mb-4"><Form.Label>Categoría</Form.Label><Form.Select value={categoria} onChange={e=>onCategoria(e.target.value)}><option value="">Todas las categorías</option>{categorias.map(c=><option key={c.id} value={c.nombre}>{c.nombre}</option>)}</Form.Select></Form.Group><Form.Group><Form.Label>Ordenar por</Form.Label><Form.Select value={orden} onChange={e=>onOrden(e.target.value)}><option value="fecha_DESC">Más recientes</option><option value="precio_ASC">Menor precio</option><option value="precio_DESC">Mayor precio</option><option value="nombre_ASC">Nombre A-Z</option></Form.Select></Form.Group></aside>}
