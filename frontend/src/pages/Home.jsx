import { useContext } from "react"; import { Alert,Col,Container,Row,Spinner } from "react-bootstrap"; import { Link } from "react-router-dom"; import { ProductContext } from "../context/ProductContext.jsx"; import HeroBanner from "../components/HeroBanner.jsx"; import CategoriaItem from "../components/CategoriaItem.jsx"; import ProductCard from "../components/ProductCard.jsx"; import TrustBar from "../components/TrustBar.jsx";
export default function Home(){const {productos,categorias,cargando,error}=useContext(ProductContext);if(cargando)return <div className="loading-screen"><Spinner animation="border"/></div>;return <>{error&&<Container className="mt-4"><Alert variant="danger">{error}</Alert></Container>}<Container fluid="xl" className="home-container"><HeroBanner/><section className="section-block"><div className="section-header"><div><h2>Explora por categorías</h2><p>Encuentra productos por tipo.</p></div><Link to="/productos">Ver todas →</Link></div><div className="categories-grid">{categorias.slice(0,4).map(c=><CategoriaItem key={c.id} {...c}/>)}</div></section><section className="section-block"><div className="section-header"><h2>Productos recomendados</h2><Link to="/productos">Ver todas →</Link></div><Row className="g-4">{productos.slice(0,4).map(p=><Col xs={12} md={6} lg={3} key={p.id}><ProductCard {...p}/></Col>)}</Row></section></Container><TrustBar/></>}
